<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="stylesheet" href="/style.css">
    {{{_sections.css}}}
    <title>Online Auction</title>
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar"
                    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbar">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item me-5">
                            <a class="nav-link active text-nowrap" aria-current="page" href="/"><i
                                    class="bi bi-house me-2"></i>Trang
                                chủ</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown"
                                data-bs-auto-close="outside" aria-expanded="false">
                                Danh mục sản phẩm
                            </a>
                            <ul class="dropdown-menu">
                                {{#each parentList}}
                                <li class="nav-item dropend">
                                    <div class="btn-group">
                                        <a class="nav-link p-2" href="/product/cat/{{category_id}}" aria-expanded="false">
                                            {{category_name}}
                                        </a>
                                        {{#if subCats}}
                                        <button type="button" class="btn dropdown-toggle dropdown-toggle-split"
                                            data-bs-toggle="dropdown" aria-expanded="false">
                                            <span class="visually-hidden">Toggle Dropdown</span>
                                        </button>
                                        <ul class="dropdown-menu">
                                            {{#each subCats}}
                                            <li><a class="dropdown-item"
                                                    href="/product/cat/{{category_id}}">{{category_name}}</a></li>
                                            {{/each}}
                                        </ul>
                                        {{/if}}
                                    </div>
                                </li>
                                {{/each}}
                            </ul>
                        </li>
                    </ul>
                    {{#if isAuthenticated}}
                    <ul class="navbar-nav me-2">
                        <li class="nav-item">
                            <div class="dropdown">
                                <i class="bi bi-person-circle fs-3" type="button"
                                data-bs-toggle="dropdown" aria-expanded="false"></i>
                                <ul class="dropdown-menu">
                                    <li><a class="dropdown-item" href="/account/profile">Xem profile</a></li>
                                    <li><a class="dropdown-item" href="/logout">Đăng xuất</a></li>
                                </ul>
                            </div>
                        </li>
                    </ul>
                    {{else}}
                    <ul class="navbar-nav me-2">
                        <li class="nav-item">
                            <a class="nav-link text-nowrap" href="/login">Đăng nhập</a>
                        </li>
                    </ul>
                    {{/if}}
                </div>
                <form class="d-flex" role="search" action="/product/results">
                    <input class="form-control me-2" type="search" placeholder="Tìm sản phẩm" aria-label="Search"
                        name="item" />
                    <button class="btn btn-primary text-nowrap" type="submit">Tìm Kiếm</button>
                </form>
            </div>
        </nav>
    </header>

    {{{body}}}

    <footer class="container-fluid p-5 bg-light gap-3">
        <div class="row">
            <div class="col-12 col-md-4 mb-3">
                <p class="fw-bold">Bidder</p>
                <ul class="nav flex-column">
                    <li class="nav-item mb-2"><a href="#" class="nav-link p-0 text-body-secondary">Đăng ký</a></li>
                    <li class="nav-item mb-2"><a href="#" class="nav-link p-0 text-body-secondary">Nâng cấp thành
                            seller</a>
                    </li>
                </ul>
            </div>
            <div class="col-12 col-md-4 mb-3">
                <p class="fw-bold">Seller</p>
                <ul class="nav flex-column">
                    <li class="nav-item mb-2"><a href="#" class="nav-link p-0 text-body-secondary">Mở bán</a></li>
                </ul>
            </div>
        </div>
        <div class="py-4 my-4 border-top">
            <p>© Online Auction.</p>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
        crossorigin="anonymous"></script>

    {{{_sections.script}}}
</body>

</html>